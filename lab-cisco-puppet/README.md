Puppet with Cisco devices
=========================

This lab features the use of `puppet device` with Cisco devices. On
Cisco, those commands should be run first:

    conf t
    crypto key generate rsa encryption exportable modulus 1024
