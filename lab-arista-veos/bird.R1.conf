log "/var/log/bird.log" all;
router id 203.0.113.11;
protocol device {
   scan time 10;
}
protocol kernel {
   persist;
   import none;
   export all;
}

protocol ospf CORE {
  import all;
  export none;
  area 0.0.0.0 {
    networks {
      203.0.113.0/24;
    };
    interface "eth0";
  };
}
